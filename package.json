{
  "name": "sistetecni-pos",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w apps/frontend\"",
    "build": "npm run build -w server && npm run build -w apps/frontend",
    "test": "npm run test -w server",
    "prisma:migrate": "npm run prisma:migrate -w server",
    "prisma:seed": "npm run prisma:seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
